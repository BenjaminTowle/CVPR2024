model:
  load_path: facebook/sam-vit-base
  save_path: dummy
  type: mcl  # Options: seqsam, det, mcl
  ablation: none  # Options: none, random, sequential, no_ha, sg
  num_samples: 3

params:
  learning_rate: 1e-4
  num_train_epochs: 1
  seed: 42
  batch_size: 2

data:
  path: ${hydra:runtime.cwd}/data
  dataset: lidc

mode: eval
debug: true

